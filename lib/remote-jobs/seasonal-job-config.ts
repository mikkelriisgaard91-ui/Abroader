/**
 * Teamtailor department name aliases for `/hospitality` (Seasonal Jobs).
 * First match in API `/departments` list order wins (see findDepartmentIdByNames).
 */
export const SEASONAL_JOB_DEPARTMENT_NAMES = [
  "Seasonal Jobs",
  "Seasonal",
  "Hospitality",
] as const;
